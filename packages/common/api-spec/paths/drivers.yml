get:
  summary: Get Multiple Drivers
  operationId: getDrivers
  description: This is a test of a description I don't know if this will work
  tags:
    - Drivers
  responses:
    200:
      description: The specified resource was not found.
      content:
        application/json:
          schema:
            type: object
            properties:
              code:
                type: integer
                description: The HTTP status code.
                minimum: 400
                maximum: 599
              description:
                type: string
                description: A short description of the cause of the error.
            required:
              - code
              - description
    404:
      description: The specified resource was not found.
      content:
        application/json:
          schema:
            type: object
            properties:
              code:
                type: integer
                description: The HTTP status code.
                minimum: 400
                maximum: 599
              description:
                type: string
                description: A short description of the cause of the error.
            required:
              - code
              - description
